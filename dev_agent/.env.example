# V3 Batch Agent Configuration

# API Configuration (필수)
HUB_API_URL=http://u24.techb.kr:3331  # HTTPS 아님 주의!
AGENT_ID=agent-02  # 각 에이전트마다 고유한 ID 사용 (agent-01, agent-02, agent-03...)

# Batch Processing
BATCH_MAX_PAGES=5     # 최대 검색 페이지 수
BATCH_SIZE=10         # 한 번에 처리할 키워드 수
BATCH_DELAY=5000      # 배치 간 대기 시간 (ms)

# Logging
LOG_LEVEL=info

# 실행 방법:
# 1. cp .env.example .env
# 2. .env 파일에서 AGENT_ID를 고유값으로 변경
# 3. ./run-batch-api.sh        # 기본 2개 키워드
# 4. ./run-batch-api.sh 100    # 100개 키워드