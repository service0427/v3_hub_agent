# ParserHub V3 Hub Server Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=8445
SOCKET_PORT=8446
LOG_LEVEL=debug
LOG_DIR=./logs

# Database Configuration (PostgreSQL) - 운영 DB
DB_HOST=mkt.techb.kr
DB_PORT=5432
DB_USER=techb_pp
DB_PASS=Tech1324!
DB_NAME=productparser_db

# Database Pool Settings
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# API Configuration
DEFAULT_API_KEY=test-api-key-123

# Agent Configuration
AGENT_TIMEOUT_MS=30000
MAX_CONCURRENT_TASKS=10
MAX_AGENTS_PER_BROWSER=5

# Browser Support (OS-specific)
# Linux: chrome,firefox (Edge not supported)
# Windows: chrome,firefox,edge
SUPPORTED_BROWSERS=chrome,firefox

# Security
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Billing
BILLING_AMOUNT_PER_QUERY=30

# CORS (for future web frontend)
CORS_ORIGIN=http://localhost:5173

# Timezone
TZ=Asia/Seoul